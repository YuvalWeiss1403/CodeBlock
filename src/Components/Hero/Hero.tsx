import "./Hero.css";

const Hero: React.FC = () => {
	return (
		<>
			<div className="hero">
				<div className="hero-title">Choose your code block</div>
			</div>
		</>
	);
};

export default Hero;
